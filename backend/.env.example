# Backend environment example
PORT=5000
# Comma-separated list of allowed origins for CORS
# Include local dev and your deployed frontend URL
CORS_ORIGIN=http://localhost:5173,https://frontend-l7s6qlma7-saugats-projects-64133a90.vercel.app

MONGODB_URI=
JWT_SECRET=change_me

# Email via SMTP (used by contact form). If unset, dev fallback logs message JSON.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
# true or false
SMTP_SECURE=false

# Optional overrides
MAIL_FROM=no-reply@hamro-chiya-pasal.local
MAIL_TO=

# Deprecated (replaced by SMTP_* above)
# EMAIL_HOST=
# EMAIL_PORT=
# EMAIL_USER=
# EMAIL_PASS=
